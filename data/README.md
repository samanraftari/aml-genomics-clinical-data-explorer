# Data directory

`raw/` is reserved for original source files and `processed/` for reproducible cleaned outputs.

Do not add private patient data. Before committing any downloaded public file, confirm that redistribution is permitted and document its source, study identifier, access date, and citation.
